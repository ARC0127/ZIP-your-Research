# Formula compatibility — portable rewrite (compact)

This is a compact rewrite of `artifact_tool_spreadsheet_formulas.md`.

Key policy:
- Keep formulas simple and widely supported.
- Avoid dynamic-array-only functions unless guaranteed.
- Verify non-trivial functions in the target spreadsheet application.
