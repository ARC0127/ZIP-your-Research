# Rewrites

This folder contains ZYR-aligned rewrites of the platform runtime files under `/home/oai/skills/**`.

Two layers are provided:

- `runtime_rw_20260222/` — compact, topic-based summaries.
- `runtime_rw_20260222_f28/` — full 1:1 coverage of all 28 platform files, mirroring the platform directory structure.

All content here is English-only to keep prompt artifacts consistent.
