# README Rewrite Audit v1.6

## Applied writing skills

- `S640_s340_global_paper_logic_language_audit`: used as the global writing and logic gate.
- `S601_paper_story_section_architecture`: used to restructure README into purpose, architecture, v1.6 additions, usage, repository map, preservation policy, attribution, and validation.
- `S602_claim_evidence_reverse_outline_review`: used to check that each claim in the README has a repository location, validation artifact, or explicit boundary.
- `S603_bilingual_human_voice_delta_rewrite`: used to remove awkward internal phrasing and rewrite for GitHub readers.

## Main corrections

1. Replaced the previous package-oriented README tone with a public repository README structure.
2. Removed confusing wording around `external_sources`; the active source-preservation path is now consistently `ext/src/`.
3. Clarified that previous integrated skills remain active and are not moved into a history-only folder.
4. Explained `S340 v4.2` as a user-authored Style & Logic Review ruleset rather than a GitHub dependency.
5. Added must-route rules for writing, figure, and no-omission tasks.
6. Added a source-preservation policy and validation section aligned with the no-delete overlay requirement.

## Forbidden/high-risk phrase check

Checked and removed/avoided:

- mechanical three-part slogans such as `trackable, reviewable, and continuously actionable`;
- repeated `not A but B` / `不是……而是……` structures;
- empty `from A to B` transitions;
- decorative use of `bridge`, `load-bearing knob`, `recipe`, or `criterion`;
- unsupported claims such as universal `robust`, `guaranteed`, or `always` language.

## Boundary

This rewrite updates README text only. It does not delete files, move active skills into legacy directories, or change the source-preservation structure.
